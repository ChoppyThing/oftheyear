# deploy/.env.example - example production env

# Frontend build-time
NEXT_PUBLIC_API_URL=https://api.oftheyear.net

# Backend runtime
DATABASE_URL=postgresql://user:password@db:5432/oftheyear
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=oftheyear

# Optional: image tag to use in docker-compose
TAG=latest
